export const userName = (state) => state.user?.user?.name
export const token = (state) => state.user?.token
export const user = (state) => state?.user?.user
export const selectUserRole = (state) => state?.user?.user?.role
export const userList = (state) => state?.user?.users
export const totalRecords = (state) => state?.user?.total
